[project]
name = "ledgerlift-backend"
version = "0.1.0"
description = "LedgerLift local-first bookkeeping cleanup API"
requires-python = ">=3.10"

[tool.uvicorn]
app = "app.main:app"
host = "0.0.0.0"
port = 8000
reload = true
